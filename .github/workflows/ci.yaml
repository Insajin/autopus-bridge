name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.25.x"

      - name: Lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.1.6
          install-mode: goinstall
          only-new-issues: true

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test -v ./...

      - name: Cross-compilation check (linux/amd64)
        env:
          GOOS: linux
          GOARCH: amd64
        run: go build -o /dev/null .

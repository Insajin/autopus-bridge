version: 2
project_name: autopus-bridge

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}

archives:
  - formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'

brews:
  - repository:
      owner: insajin
      name: homebrew-tap
      # 크로스 리포 push를 위해 PAT가 필요합니다.
      # GitHub repo secrets에 HOMEBREW_TAP_TOKEN을 추가하세요.
    homepage: https://github.com/insajin/autopus-bridge
    description: "Autopus Local Agent Bridge - Connect local development agents to Autopus platform"
    license: "MIT"
    install: |
      bin.install "autopus-bridge"
    test: |
      system "#{bin}/autopus-bridge", "version"
